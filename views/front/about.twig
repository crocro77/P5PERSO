{% extends 'template/layout.twig' %}

{% block content %}
<br />
<div id="about" class="center about-container grey lighten-3">
	<div class="container">
		<div class="page-header">
			<h3>À propos</h3>
		</div>
		{% if not aboutDescription %}
            <p>Cette page n'a pas encore été complétée.</p>
            {% if _session.username == 'ntonyyy' %}
                <p><a href="admin/settings">Le faire maintenant.</a></p>
            {% endif %}
        {% else %}
            {{ aboutDescription }}
        {% endif %}
	</div>
</div>

{% endblock %}